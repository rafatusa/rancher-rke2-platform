# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '17'
    - name: Set up Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: 8.10.2
    - name: Compile sources
      run: gradle --no-daemon compileJava compileTestJava compileIntegrationTestJava
      working-directory: app
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '17'
    - name: Set up Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: 8.10.2
    - name: Unit tests + JaCoCo coverage verification
      run: gradle --no-daemon test jacocoTestReport jacocoTestCoverageVerification
      working-directory: app
    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      with:
        name: jacoco-report
        path: app/build/reports/jacoco/test
  build:
    runs-on: ubuntu-latest
    needs:
    - test
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '17'
    - name: Set up Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: 8.10.2
    - name: Gradle bootJar
      run: gradle --no-daemon bootJar
      working-directory: app
    - name: Upload jar
      uses: actions/upload-artifact@v4
      with:
        name: employee-api-jar
        path: app/build/libs/employee-api.jar
  image:
    runs-on: ubuntu-latest
    needs:
    - build
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - name: Log in to GHCR
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - id: imageref
      name: Compute image reference
      run: 'OWNER=$(echo "${{ github.repository_owner }}" | tr ''[:upper:]'' ''[:lower:]'')

        echo "image=ghcr.io/${OWNER}/employee-api" >> "$GITHUB_OUTPUT"

        echo "tag=${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"

        '
    - name: Build and push image
      uses: docker/build-push-action@v6
      with:
        context: ./app
        push: true
        tags: '${{ steps.imageref.outputs.image }}:${{ steps.imageref.outputs.tag
          }}

          ${{ steps.imageref.outputs.image }}:latest

          '
        cache-from: type=gha
        cache-to: type=gha,mode=max
  provision:
    runs-on: ubuntu-latest
    needs:
    - image
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
    timeout-minutes: 45
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - name: Terraform validate
      run: terraform validate
      working-directory: infra
    - name: Terraform apply
      run: terraform apply -input=false -auto-approve
      working-directory: infra
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      ANSIBLE_HOST_KEY_CHECKING: 'False'
    timeout-minutes: 120
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - name: Install Ansible
      run: 'python3 -m pip install --upgrade pip

        python3 -m pip install "ansible-core>=2.16,<2.18"

        ansible-galaxy collection install community.general ansible.posix

        ansible --version

        '
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - id: tfout
      name: Read master IP
      run: echo "master_ip=$(terraform output -raw master_public_ip)" >> "$GITHUB_OUTPUT"
      working-directory: infra
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    - name: Wait for SSH on the master
      run: "for i in $(seq 1 40); do\n  if ssh-keyscan -T 10 -H \"$MASTER_IP\" >>\
        \ ~/.ssh/known_hosts 2>/dev/null; then\n    if grep -q \"$MASTER_IP\" ~/.ssh/known_hosts;\
        \ then\n      echo \"sshd is up on the master\"\n      exit 0\n    fi\n  fi\n\
        \  echo \"waiting for sshd on the master ($i/40)\"\n  sleep 15\ndone\necho\
        \ \"master did not become reachable over SSH\" >&2\nexit 1\n"
      env:
        MASTER_IP: ${{ steps.tfout.outputs.master_ip }}
    - name: Generate Ansible inventory
      run: 'python3 ../scripts/gen_inventory.py > ../ansible/inventory.ini

        grep -v ''rke2_token'' ../ansible/inventory.ini

        '
      working-directory: infra
      env:
        SSH_USER: ${{ secrets.SSH_USER }}
    - name: Run Ansible playbook
      run: "ansible-playbook -i inventory.ini site.yml \\\n  -e \"rancher_bootstrap_password=${RANCHER_BOOTSTRAP_PASSWORD}\"\
        \n"
      working-directory: ansible
      env:
        RANCHER_BOOTSTRAP_PASSWORD: ${{ secrets.RANCHER_BOOTSTRAP_PASSWORD }}
  release:
    runs-on: ubuntu-latest
    needs:
    - configure
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - id: tfout
      name: Read master IP
      run: echo "master_ip=$(terraform output -raw master_public_ip)" >> "$GITHUB_OUTPUT"
      working-directory: infra
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        ssh-keyscan -T 20 -H "$MASTER_IP" >> ~/.ssh/known_hosts

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        MASTER_IP: ${{ steps.tfout.outputs.master_ip }}
    - name: Copy the Helm chart to the master
      run: "ssh -i ~/.ssh/deploy_key \"$SSH_USER@$MASTER_IP\" \\\n  \"rm -rf /tmp/employee-api-chart\
        \ && mkdir -p /tmp/employee-api-chart\"\nscp -i ~/.ssh/deploy_key -r charts/employee-api/.\
        \ \\\n  \"$SSH_USER@$MASTER_IP:/tmp/employee-api-chart/\"\n"
      env:
        SSH_USER: ${{ secrets.SSH_USER }}
        MASTER_IP: ${{ steps.tfout.outputs.master_ip }}
    - name: Deploy with Helm and wait for the rollout
      run: "OWNER=$(echo \"${{ github.repository_owner }}\" | tr '[:upper:]' '[:lower:]')\n\
        IMAGE=\"ghcr.io/${OWNER}/employee-api\"\nTAG=\"${GITHUB_SHA::12}\"\n# The\
        \ registry credential is piped over stdin so it never appears in\n# the remote\
        \ process list or shell history.\nprintf '%s\\n' \"$REGISTRY_CREDENTIAL\"\
        \ | ssh -i ~/.ssh/deploy_key \"$SSH_USER@$MASTER_IP\" \\\n  \"sudo /usr/local/bin/deploy-employee-api.sh\
        \ '$IMAGE' '$TAG' '$REGISTRY_USER' '$MASTER_IP'\"\n"
      env:
        SSH_USER: ${{ secrets.SSH_USER }}
        MASTER_IP: ${{ steps.tfout.outputs.master_ip }}
        REGISTRY_USER: ${{ github.actor }}
        REGISTRY_CREDENTIAL: ${{ secrets.GITHUB_TOKEN }}
  verify:
    runs-on: ubuntu-latest
    needs:
    - provision
    - configure
    - release
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - id: tfout
      name: Read master IP
      run: echo "master_ip=$(terraform output -raw master_public_ip)" >> "$GITHUB_OUTPUT"
      working-directory: infra
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        ssh-keyscan -T 20 -H "$MASTER_IP" >> ~/.ssh/known_hosts

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        MASTER_IP: ${{ steps.tfout.outputs.master_ip }}
    - name: Kubernetes cluster health checks
      run: ssh -i ~/.ssh/deploy_key "$SSH_USER@$MASTER_IP" "sudo /usr/local/bin/cluster-health.sh"
      env:
        SSH_USER: ${{ secrets.SSH_USER }}
        MASTER_IP: ${{ steps.tfout.outputs.master_ip }}
    - name: Employee API is healthy through the ingress
      run: "curl --fail --show-error --silent --retry 20 --retry-delay 15 --retry-all-errors\
        \ \\\n  \"http://api.${MASTER_IP}.sslip.io/health\"\necho\n"
      env:
        MASTER_IP: ${{ steps.tfout.outputs.master_ip }}
    - name: Rancher UI is reachable
      run: "curl --fail --show-error --silent --insecure --retry 20 --retry-delay\
        \ 15 \\\n  --retry-all-errors -o /dev/null \"https://rancher.${MASTER_IP}.sslip.io/healthz\"\
        \necho \"Rancher healthz OK\"\n"
      env:
        MASTER_IP: ${{ steps.tfout.outputs.master_ip }}
    - name: Deployment summary
      run: "{\n  echo \"### Rancher RKE2 Platform is live\"\n  echo \"\"\n  echo \"\
        | Endpoint | URL |\"\n  echo \"| --- | --- |\"\n  echo \"| Rancher UI | https://rancher.${MASTER_IP}.sslip.io\
        \ |\"\n  echo \"| Employee API | http://api.${MASTER_IP}.sslip.io |\"\n  echo\
        \ \"| Health | http://api.${MASTER_IP}.sslip.io/health |\"\n  echo \"| Employees\
        \ | http://api.${MASTER_IP}.sslip.io/employees |\"\n  echo \"\"\n  echo \"\
        Cluster: 1 RKE2 server + 3 RKE2 agents.\"\n} >> \"$GITHUB_STEP_SUMMARY\"\n"
      env:
        MASTER_IP: ${{ steps.tfout.outputs.master_ip }}
