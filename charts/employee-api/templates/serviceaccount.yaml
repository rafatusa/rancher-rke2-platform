{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "employee-api.serviceAccountName" . }}
  labels:
    {{- include "employee-api.labels" . | nindent 4 }}
# The application needs no Kubernetes API access.
automountServiceAccountToken: false
{{- end }}
