Employee API has been deployed.

  Release:   {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}
  Image:     {{ .Values.image.repository }}:{{ .Values.image.tag }}
  Replicas:  {{ .Values.replicaCount }}

{{- if .Values.ingress.enabled }}

Reachable at:

  http://{{ .Values.ingress.host }}/           landing page
  http://{{ .Values.ingress.host }}/health     health probe
  http://{{ .Values.ingress.host }}/employees  employee collection
{{- end }}

Check the rollout:

  kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ include "employee-api.fullname" . }}
  kubectl -n {{ .Release.Namespace }} get pods -o wide
